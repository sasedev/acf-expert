						<a href="#" class="dropdown-toggle" data-toggle="dropdown">
							<i class="fa fa-bell-o"></i><span class="label label-danger">{{ nextEvents|length }}</span> Agenda <i class="caret"></i></span>
						</a>
						<ul class="dropdown-menu">
							{% if nextEvents|length != 0 %}
							<li class="header">Prochains évènements (sur 10j)</li>
							{% for evt in nextEvents %}
							<li>
								<ul class="menu">
									<li>
										<a href="{{path('_security_agenda_event', {'uid':evt.id})}}">
											<div class="pull-left">
												<i class="fa fa-user fa-2x{% if evt.user.id != app.user.id %} text-red{% endif %}"></i>
											</div>
											<h6> &nbsp; {{ evt.user.fullName }}</h6>
											<div class="text-right"><i class="fa fa-clock-o"></i> {{ evt.dtStart|date('Y/m/d @ H:i') }}</div>
											<div class="clear"></div>
											<p>{{ evt.title }}</p>
										</a>
									</li>
								</ul>
							</li>
							{% endfor %}
							{% else %}
							<li class="header">Aucun évènement dans les prochains jours</li>
							{% endif %}
							<li class="footer"><a href="{{path('_security_agenda_index')}}" class="btn">{{ 'menu.agenda'|trans|raw }} Voir Agenda</a>
							</li>
						</ul>